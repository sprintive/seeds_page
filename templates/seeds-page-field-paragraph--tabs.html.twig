<div {{attributes.addClass(classes, 'tabs')}}>
    <ul class = 'tabs-wrapper'>
    {% for tab in tabs %}
        <li>{{ tab }}</li>
    {% endfor %}
    </ul>
    {% for item in items %}
        <div class = 'field--item'>{{ item.content }}</div>
    {% endfor %}
</div>