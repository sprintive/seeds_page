<div class="seeds-page-banner">
  <div class="seeds-banner">{{image}}</div>
  {% if show_title %}
    <div class="container">
      <h1 class="page-header">{{title}}</h1>
    </div>
  {% endif %}
</div>
